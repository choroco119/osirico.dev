# スマートフォン操作（タップ位置ズレ・スワイプ移動）の修正タスク

- [ ] `script.js` の座標計算ロジックを更新
    - [ ] `getCanvasCoords` ヘルパー関数の導入（CSSの倍率 `scale` を考慮）
    - [ ] `handleMouseDown` でのヒット判定に「倍率」を適用
    - [ ] タッチ操作時の「当たり判定」の緩和（数ピクセルのマージン追加）
- [ ] スワイプ（ドラッグ）移動の安定化
    - [ ] `handleMouseMove` での移動量計算に「倍率」を適用
    - [ ] 選択済みパーツがある場合に、指の動きとパーツの動きを同期させる
- [ ] 動作確認（ブラウザのデベロッパーツールで各種倍率での挙動を確認）
- [ ] `walkthrough.md` の更新
